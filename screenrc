# ~/.screenrc - GNU Screen configuration (works on Linux and macOS).

# Keep a long scrollback buffer (lines kept per window for scrolling back).
defscrollback 10000

# Don't show the startup copyright message.
startup_message off

# Use a visual bell instead of an audible beep.
vbell on
